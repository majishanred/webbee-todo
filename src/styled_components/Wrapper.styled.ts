import styled from 'styled-components';

export const StyledWrapper = styled('section')`
  display: flex;
  flex-direction: column;

  width: 100%;
  max-width: 800px;
  margin: 60px auto;

  gap: 20px;
`;
